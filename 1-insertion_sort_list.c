#include "sort.h"

void insertion_sort_list(listint_t **list)
{
	int temp = 0, i, j;
	listint_t *aux, *current;
	
	current = (*list)->next;

	while (current != NULL)
	{
		aux = current;
		while ()
		{
		
		}
	}
}
